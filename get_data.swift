import Foundation

func fetchFritz() async {
    let router = "192.168.178.1" // Default, or read from defaults
    // Since we don't have the auth, it's hard. But we can just see if we can instantiate TR064HostService from the app bundle? No, it's not a framework.
}
